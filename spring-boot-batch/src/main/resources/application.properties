#spring.data.mongodb.host=localhost
#spring.data.mongodb.port=27017
#spring.data.mongodb.database=dbname
logging.level.*=ERROR

#file location
common.folder=C:/TEST/

#Table names for each section in the order as a temporary solution.
#The ideal solution would be to store this data in a table and query it in the processor 
dbViewList=employee_address,employee_benefits

# no of records to be processed by each thread will be determined by fromId and to toId
fromId=1
toId=900
#no of threads will be determined by grid size
gridSize=1

# DataSource settings: set here configurations for the database connection.		
spring.datasource.url = jdbc:mysql://localhost:3306/demo
spring.datasource.username = root
spring.datasource.password = test
spring.datasource.driverClassName = com.mysql.jdbc.Driver

